{
  "compilerOptions": {
    // --- База ---
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,

    // --- Выходные файлы ---
    "outDir": "./dist",
    
    // --- JSX (если используете, на всякий случай) ---
    "jsx": "react-jsx" 
  },
  "include": ["src"], // Компилируем только папку src
  "exclude": ["node_modules", "dist"] // Исключаем все остальное
}